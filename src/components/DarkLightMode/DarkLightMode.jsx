import { NavLink } from 'react-router-dom';

export const DarkLightMode = () => {
    return (
        <div>
            <NavLink>
                <button>Dark/Light</button>
            </NavLink>
        </div>
    );
};